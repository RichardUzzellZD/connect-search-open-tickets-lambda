"""\nzendesk_oauth.py - OAuth token management for Zendesk API\n\nThis module provides OAuth 2.0 Client Credentials flow authentication for\nAWS Lambda functions integrating with Zendesk.\n\nFeatures:\n- Automatic token refresh when expired\n- Token caching in AWS Secrets Manager\n- Retry logic for 401 errors\n- Thread-safe token management\n\nUsage:\n    from zendesk_oauth import make_zendesk_request\n\n    response = make_zendesk_request(\n        'POST',\n        'https://subdomain.zendesk.com/api/v2/tickets.json',\n        'zendesk/oauth/connect-lambda',\n        scopes='tickets:write',\n        json={'ticket': {'subject': 'Test', 'comment': {'body': 'Test'}}}\n    )\n\nEnvironment Variables:\n    ZENDESK_OAUTH_SECRET_NAME: AWS Secrets Manager secret name containing OAuth credentials\n\nSecrets Manager Secret Format:\n    {\n        \"client_id\": \"z3n_oauth_client_abc123\",\n        \"client_secret\": \"your-client-secret\",\n        \"subdomain\": \"your-subdomain\",\n        \"access_token\": \"\",\n        \"token_expiry\": 0\n    }\n\nAuthor: Richard Uzzell\nDate: 2026-07-24\nVersion: 1.0\n\"\""\n\nimport json\nimport os\nimport time\nimport boto3\nimport requests\nimport logging\nfrom typing import Dict, Any, Optional\nfrom botocore.exceptions import ClientError\n\nlogger = logging.getLogger()\nif not logger.handlers:\n    logger.setLevel(os.environ.get('LOG_LEVEL', 'INFO').upper())\n\n# Initialize AWS clients (reused across Lambda invocations)\nsecrets_client = boto3.client('secretsmanager')\n\n\ndef get_oauth_credentials(secret_name: str) -> Dict[str, Any]:\n    \"\"\"\n    Retrieve OAuth credentials from AWS Secrets Manager.\n\n    Args:\n        secret_name: AWS Secrets Manager secret name\n\n    Returns:\n        Dictionary with client_id, client_secret, subdomain, access_token, token_expiry\n\n    Raises:\n        ClientError: If secret not found or access denied\n        ValueError: If secret format is invalid\n    \"\"\"\n    try:\n        logger.debug(f\"Retrieving OAuth credentials from secret: {secret_name}\")\n        response = secrets_client.get_secret_value(SecretId=secret_name)\n        credentials = json.loads(response['SecretString'])\n\n        # Validate required fields\n        required_fields = ['client_id', 'client_secret', 'subdomain']\n        missing_fields = [f for f in required_fields if not credentials.get(f)]\n\n        if missing_fields:\n            raise ValueError(f\"Missing required fields in secret: {', '.join(missing_fields)}\")\n\n        logger.debug(f\"Successfully retrieved credentials for subdomain: {credentials.get('subdomain')}\")\n        return credentials\n\n    except ClientError as e:\n        error_code = e.response['Error']['Code']\n        logger.error(f\"Failed to retrieve OAuth credentials: {error_code}\")\n\n        if error_code == 'ResourceNotFoundException':\n            raise ValueError(f\"Secret not found: {secret_name}\")\n        elif error_code == 'AccessDeniedException':\n            raise PermissionError(f\"Access denied to secret: {secret_name}. Check Lambda IAM role permissions.\")\n        else:\n            raise\n\n    except json.JSONDecodeError as e:\n        logger.error(f\"Invalid JSON in secret {secret_name}: {str(e)}\")\n        raise ValueError(f\"Secret contains invalid JSON: {str(e)}\")\n\n\ndef request_access_token(client_id: str, client_secret: str,\n                         subdomain: str, scopes: str = \"tickets:write users:read\") -> Dict[str, Any]:\n    \"\"\"\n    Request a new OAuth access token using client credentials flow.\n\n    Args:\n        client_id: OAuth client identifier\n        client_secret: OAuth client secret\n        subdomain: Zendesk subdomain (e.g., 'mycompany')\n        scopes: Space-separated list of scopes (e.g., \"tickets:write users:read\")\n\n    Returns:\n        Token response dict with access_token, expires_in, token_type, scope\n\n    Raises:\n        requests.RequestException: If token request fails\n\n    Example:\n        token = request_access_token(\n            'client_id',\n            'client_secret',\n            'mycompany',\n            'tickets:write users:read'\n        )\n        # Returns: {\n        #   \"access_token\": \"eyJ0eXAi...\",\n        #   \"token_type\": \"Bearer\",\n        #   \"expires_in\": 1800,\n        #   \"scope\": \"tickets:write users:read\"\n        # }\n    \"\"\"\n    url = f\"https://{subdomain}.zendesk.com/oauth/tokens\"\n\n    payload = {\n        \"grant_type\": \"client_credentials\",\n        \"client_id\": client_id,\n        \"client_secret\": client_secret,\n        \"scope\": scopes\n    }\n\n    logger.info(f\"Requesting OAuth access token from {subdomain}.zendesk.com\")\n    logger.debug(f\"Requested scopes: {scopes}\")\n\n    try:\n        response = requests.post(url, data=payload, timeout=10)\n        response.raise_for_status()\n\n        token_data = response.json()\n\n        # Validate response format\n        if 'access_token' not in token_data:\n            raise ValueError(\"Token response missing 'access_token' field\")\n\n        expires_in = token_data.get('expires_in', 1800)\n        granted_scopes = token_data.get('scope', 'unknown')\n\n        logger.info(f\"Successfully obtained access token (expires in {expires_in}s)\")\n        logger.debug(f\"Granted scopes: {granted_scopes}\")\n\n        return token_data\n\n    except requests.exceptions.HTTPError as e:\n        status_code = e.response.status_code if e.response else 'unknown'\n        error_text = e.response.text if e.response else 'no response'\n\n        logger.error(f\"Failed to obtain access token: HTTP {status_code}\")\n        logger.error(f\"Response: {error_text}\")\n\n        if status_code == 401:\n            raise PermissionError(\"Invalid client credentials. Check client_id and client_secret.\")\n        elif status_code == 400:\n            raise ValueError(f\"Invalid token request: {error_text}\")\n        else:\n            raise\n\n    except requests.exceptions.Timeout:\n        logger.error(f\"Token request timed out after 10 seconds\")\n        raise\n\n    except requests.exceptions.RequestException as e:\n        logger.error(f\"Token request failed: {str(e)}\")\n        raise\n\n\ndef update_secret_with_token(secret_name: str, credentials: Dict[str, Any],\n                             access_token: str, expires_in: int) -> None:\n    \"\"\"\n    Update Secrets Manager with new access token and expiry time.\n\n    Args:\n        secret_name: AWS Secrets Manager secret name\n        credentials: Full credentials dictionary (will be updated in place)\n        access_token: New access token\n        expires_in: Token lifetime in seconds\n\n    Raises:\n        ClientError: If secret update fails\n\n    Notes:\n        - Subtracts 60s safety buffer from expires_in to avoid using expired tokens\n        - Updates credentials dict in place (mutates input)\n        - Logs token expiry timestamp for debugging\n    \"\"\"\n    # Calculate expiry timestamp (subtract 60s buffer for safety)\n    token_expiry = int(time.time()) + expires_in - 60\n\n    credentials['access_token'] = access_token\n    credentials['token_expiry'] = token_expiry\n\n    logger.debug(f\"Updating secret with new token (expires at Unix timestamp {token_expiry})\")\n\n    try:\n        secrets_client.update_secret(\n            SecretId=secret_name,\n            SecretString=json.dumps(credentials)\n        )\n\n        expires_in_minutes = (token_expiry - int(time.time())) / 60\n        logger.info(f\"Updated secret with new token (expires in {expires_in_minutes:.1f} minutes)\")\n\n    except ClientError as e:\n        error_code = e.response['Error']['Code']\n        logger.error(f\"Failed to update secret: {error_code}\")\n\n        if error_code == 'AccessDeniedException':\n            raise PermissionError(f\"Access denied when updating secret: {secret_name}. \"\n                                 \"Check Lambda IAM role has secretsmanager:UpdateSecret permission.\")\n        else:\n            raise\n\n\ndef get_valid_access_token(secret_name: str, scopes: str = \"tickets:write users:read\") -> str:\n    \"\"\"\n    Get a valid access token, refreshing if necessary.\n\n    This is the main function to call from your Lambda handler to get an access token.\n    It handles token caching, expiry checking, and automatic refresh.\n\n    Args:\n        secret_name: AWS Secrets Manager secret name\n        scopes: Required OAuth scopes (space-separated)\n\n    Returns:\n        Valid access token string\n\n    Raises:\n        ValueError: If secret is missing required fields\n        PermissionError: If IAM permissions insufficient\n        requests.RequestException: If token request fails\n\n    Example:\n        access_token = get_valid_access_token(\n            'zendesk/oauth/connect-lambda',\n            'tickets:write users:read'\n        )\n        headers = {'Authorization': f'Bearer {access_token}'}\n\n    Notes:\n        - Caches token in Secrets Manager to avoid requesting new token on every call\n        - Automatically refreshes token when expired\n        - Uses 60s safety buffer before expiry\n    \"\"\"\n    # Get credentials from Secrets Manager\n    credentials = get_oauth_credentials(secret_name)\n\n    client_id = credentials['client_id']\n    client_secret = credentials['client_secret']\n    subdomain = credentials['subdomain']\n    access_token = credentials.get('access_token', '')\n    token_expiry = credentials.get('token_expiry', 0)\n\n    # Check if token is still valid (with 60s buffer)\n    current_time = int(time.time())\n    time_until_expiry = token_expiry - current_time\n\n    if access_token and current_time < token_expiry:\n        logger.info(f\"Using existing access token (expires in {time_until_expiry}s)\")\n        return access_token\n\n    # Token is expired or missing, request new one\n    if access_token:\n        logger.info(f\"Access token expired {abs(time_until_expiry)}s ago, requesting new token\")\n    else:\n        logger.info(\"No access token found, requesting new token\")\n\n    token_response = request_access_token(client_id, client_secret, subdomain, scopes)\n\n    new_access_token = token_response['access_token']\n    expires_in = token_response.get('expires_in', 1800)  # Default 30 minutes\n\n    # Update secret with new token\n    update_secret_with_token(secret_name, credentials, new_access_token, expires_in)\n\n    return new_access_token\n\n\ndef make_zendesk_request(method: str, url: str, secret_name: str,\n                        scopes: str = \"tickets:write users:read\",\n                        **kwargs) -> requests.Response:\n    \"\"\"\n    Make an authenticated request to Zendesk API with automatic token refresh on 401.\n\n    This is the recommended way to make Zendesk API requests. It handles:\n    - Automatic OAuth token retrieval\n    - Token caching and refresh\n    - Retry on 401 Unauthorized (in case token was revoked)\n\n    Args:\n        method: HTTP method (GET, POST, PUT, PATCH, DELETE)\n        url: Full Zendesk API URL (e.g., 'https://subdomain.zendesk.com/api/v2/tickets.json')\n        secret_name: AWS Secrets Manager secret name\n        scopes: Required OAuth scopes (space-separated)\n        **kwargs: Additional arguments to pass to requests (json, data, params, timeout, etc.)\n\n    Returns:\n        requests.Response object\n\n    Raises:\n        ValueError: If secret is invalid\n        PermissionError: If IAM permissions insufficient\n        requests.RequestException: If request fails after retry\n\n    Example:\n        # Create a ticket\n        response = make_zendesk_request(\n            'POST',\n            'https://mycompany.zendesk.com/api/v2/tickets.json',\n            'zendesk/oauth/connect-lambda',\n            scopes='tickets:write',\n            json={\n                'ticket': {\n                    'subject': 'Test Ticket',\n                    'comment': {'body': 'This is a test'}\n                }\n            },\n            timeout=10\n        )\n\n        if response.status_code == 201:\n            ticket = response.json()['ticket']\n            print(f\"Created ticket {ticket['id']}\")\n\n    Notes:\n        - Automatically retries once on 401 (forces token refresh)\n        - Does NOT retry on other errors (caller should handle)\n        - Sets Authorization header automatically\n        - Default timeout is None (caller should set explicit timeout)\n    \"\"\"\n    # Get valid access token\n    access_token = get_valid_access_token(secret_name, scopes)\n\n    # Prepare headers\n    headers = kwargs.pop('headers', {})\n    headers['Authorization'] = f\"Bearer {access_token}\"\n\n    # Set default timeout if not provided\n    if 'timeout' not in kwargs:\n        kwargs['timeout'] = 30\n        logger.debug(\"No timeout specified, using default 30s\")\n\n    # Make request\n    logger.debug(f\"Making {method} request to {url}\")\n    response = requests.request(method, url, headers=headers, **kwargs)\n\n    # If 401, token may have been revoked - force refresh and retry once\n    if response.status_code == 401:\n        logger.warning(f\"Received 401 Unauthorized, forcing token refresh and retrying\")\n        logger.debug(f\"Response: {response.text}\")\n\n        # Force token refresh by clearing the cached token\n        credentials = get_oauth_credentials(secret_name)\n        credentials['access_token'] = ''\n        credentials['token_expiry'] = 0\n\n        # Request new token (don't update secret yet, let get_valid_access_token handle it)\n        logger.info(\"Forcing token refresh after 401\")\n        access_token = get_valid_access_token(secret_name, scopes)\n\n        # Update header with new token\n        headers['Authorization'] = f\"Bearer {access_token}\"\n\n        # Retry request\n        logger.info(f\"Retrying {method} request with new token\")\n        response = requests.request(method, url, headers=headers, **kwargs)\n\n        if response.status_code == 401:\n            logger.error(\"Still received 401 after token refresh. Check OAuth client permissions.\")\n        else:\n            logger.info(f\"Retry successful: HTTP {response.status_code}\")\n\n    return response\n\n\n# Convenience functions for common operations\n\ndef get_zendesk_subdomain(secret_name: str) -> str:\n    \"\"\"\n    Get Zendesk subdomain from Secrets Manager.\n\n    Useful for constructing API URLs.\n\n    Args:\n        secret_name: AWS Secrets Manager secret name\n\n    Returns:\n        Zendesk subdomain string\n    \"\"\"\n    credentials = get_oauth_credentials(secret_name)\n    return credentials['subdomain']\n\n\ndef build_zendesk_url(secret_name: str, endpoint: str) -> str:\n    \"\"\"\n    Build full Zendesk API URL from endpoint path.\n\n    Args:\n        secret_name: AWS Secrets Manager secret name\n        endpoint: API endpoint (e.g., '/api/v2/tickets.json')\n\n    Returns:\n        Full URL string\n\n    Example:\n        url = build_zendesk_url('zendesk/oauth/connect-lambda', '/api/v2/tickets.json')\n        # Returns: 'https://mycompany.zendesk.com/api/v2/tickets.json'\n    \"\"\"\n    subdomain = get_zendesk_subdomain(secret_name)\n\n    # Remove leading slash if present\n    endpoint = endpoint.lstrip('/')\n\n    return f\"https://{subdomain}.zendesk.com/{endpoint}\"\n\n\n# Module-level logger configuration\nif __name__ == '__main__':\n    # If run directly, set up basic logging for testing\n    logging.basicConfig(\n        level=logging.DEBUG,\n        format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'\n    )\n\n    print(\"zendesk_oauth.py - OAuth token management module\")\n    print(\"This module is intended to be imported by Lambda functions\")\n    print(\"\\nExample usage:\")\n    print(\"  from zendesk_oauth import make_zendesk_request\")\n    print(\"  response = make_zendesk_request('GET', url, secret_name)\")